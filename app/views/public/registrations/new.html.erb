<h2 class="log-sign-title mt-5">Sign up</h2>

<div class="account-intro-box">
  <p class="account-intro">
      アカウント登録していただくと、<br>
      パーク、銭湯のレビュー投稿、閲覧、<br>
      お気に入り登録も可能に！<br>
      また、他エリアに行った時のパーク、銭湯の投稿や<br>
      ユーザーのパーク、銭湯の投稿一覧を閲覧、コメントすることができます!
  </p>
</div>

  <div class="card-block">
    <div class="card col-5 mb-5">
      <div class="card-title">
        新規登録
      </div>
      <div class="card-body login-card-body">
        <%= form_with model: @user, url: user_registration_path do |f| %>
          <%= render "public/shared/error_messages", resource: @user %>
            <div class="input-group mb-3">
              <%= f.text_field :name, autofocus: true, placeholder: 'アカウントネーム(2文字以上)', class: 'form-control' %>
                <div class="input-group-append">
                  <div class="input-group-text">
                    <span class="fas fa-user"></span>
                  </div>
                </div>
            </div>
            <div class="input-group mb-3">
              <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control', placeholder: 'メールアドレス' %>
                <div class="input-group-append">
                  <div class="input-group-text">
                    <span class="fas fa-envelope"></span>
                  </div>
                </div>
            </div>
            <div class="input-group mb-3">
              <%# if @minimum_password_length %>
              <!--<em>(<%#= @minimum_password_length %> 文字以上)</em>-->
              <%# end %>
              <%= f.password_field :password, autocomplete: "new-password", class: 'form-control', placeholder: "パスワード(6文字以上)" %>
                <div class="input-group-append">
                  <div class="input-group-text">
                    <span class="fas fa-key"></span>
                  </div>
                </div>
            </div>
            <div class="input-group mb-3">
              <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control', placeholder: "パスワード確認" %>
              <div class="input-group-append">
                <div class="input-group-text">
                  <span class="fas fa-check-double"></span>
                </div>
              </div>
            </div>

            <div class='row'>
              <div class='col-12'>
                <%= f.submit "Create Account", class: "form-control btn btn-secondary mb-3" %>
              </div>
            </div>
        <% end %>
      </div>
      <%= render "public/shared/links" %>
    </div>
  </div>
