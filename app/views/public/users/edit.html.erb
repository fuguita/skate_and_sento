<h2>会員情報編集</h2>

<%= form_with model: current_user, url: users_path, method: :patch do |f| %>
  <p>ユーザーネーム<%= f.text_field :name %></p>
  <p>メールアドレス<%=f.text_field :email %> </p>
  <%= f.submit '変更を保存' %>
<% end %>
  <%= link_to '会員を退会する', unsubscribe_users_path %>
  
