<%= link_to  sentos_path, class: "back-link-park-sento" do %>
  <i class="fas fa-angle-double-left"></i>銭湯 LIST
<% end %><br>
<h3 class="favorite-park-sento-titile mt-5">
  My Favorite 銭湯
  <span id="count">
    <%= render 'public/sento_favorites/count', favorite_sentos: @favorite_sentos %>
  </span>
</h3>
  <% if @favorite_sentos.present? %>
    <div class="row favorite-box">
      <% @favorite_sentos.each do |sento| %>
        <div class="col-md-4">
          <div class="card mb-4">
              <%= image_tag sento.sento_images.first, class: 'card-img-top', height:'250' %>
              <div class="card-body">
                <h5 class="card-title mt-2 text-left">
                  <%= link_to sento_path(sento.id), class: 'favarite-park-sento-name-link' do %>
                    <%= sento.name %>
                  <% end %>
                </h5>
                <ul class="list-unstyled">
                    <li>エリア：<%= sento.prefecture_id %></li>
                </ul>
                <div id="sento-btn-<%= sento.id %>">
                  <h5><%= render 'public/sentos/sento_favorite_buttun', sento: sento %></h5>
                </div>
              </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <h4>お気にり登録はありません</h4>
  <% end %>